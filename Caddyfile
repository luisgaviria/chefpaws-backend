{
    frankenphp
    # This removes the "unformatted" warning you saw in the logs
}

# Change ":{$PORT}" to "0.0.0.0:{$PORT}"
0.0.0.0:{$PORT} {
    root * /app/web
    
    # This is the standard for Drupal 11
    php_server
    file_server
}